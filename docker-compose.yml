version: "3.9"

services:

  python-service:
    build: .
    container_name: python-service
    restart: always
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app